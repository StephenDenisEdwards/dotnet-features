# Extension Members (C# 14)

Investigation of the new extension members feature in C# 14, comparing traditional static extension methods with the new `extension(Type)` block syntax that supports extension properties and grouped declarations.
