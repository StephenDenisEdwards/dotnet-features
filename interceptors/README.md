# Interceptors

C# interceptors allow you to reroute method calls to different implementations at compile time using the `[InterceptsLocation]` attribute. This feature is primarily designed for source generators but can be used directly to explore how compile-time call-site interception works.
