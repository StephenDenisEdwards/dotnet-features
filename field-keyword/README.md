# field-keyword

This project investigates the C# 14 `field` keyword, which provides direct access to a property's compiler-generated backing field from within the property accessor. This eliminates the need for explicit backing fields when you only need them for validation or transformation logic.

IL comparison and analysis will be added after building.
