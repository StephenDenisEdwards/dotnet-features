# TimeProvider

Demonstrates the `TimeProvider` abstraction (.NET 8+) — testable time with `FakeTimeProvider` for unit tests and `TimeProvider.System` for production. Shows how to decouple code from `DateTime.Now` and `Task.Delay`.

## Run

```bash
dotnet run --project console-app
```
