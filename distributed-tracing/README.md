# Distributed Tracing

Explores `System.Diagnostics.ActivitySource` and `Activity` for distributed tracing — the BCL's built-in OpenTelemetry-compatible tracing primitives. Shows creating spans, adding tags/events, and parent-child relationships with no external packages.

## Run

```bash
dotnet run --project console-app
```
